---
name: Autolock
"on":
  workflow_dispatch:
  schedule:
   # Run weekly, every Monday at 4:43.
    - cron: "43 4 * * 1"

jobs:

  autolock:
    uses: kdeldycke/workflows/.github/workflows/autolock.yaml@v5.11.1
