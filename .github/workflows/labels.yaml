---
name: Labels
"on":
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
  issues:
    types:
      - opened

jobs:

  labels:
    uses: kdeldycke/workflows/.github/workflows/labels.yaml@main
